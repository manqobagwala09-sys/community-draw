// EVENT ENGINE (COMMUNICATION + SYNC + MODULE CONTROL)

const EventEngine = {
  events: {},

  on(event, fn) {
    (this.events[event] ||= []).push(fn);
  },

  emit(event, data) {
    (this.events[event] || []).forEach(fn => fn(data));
  },

  syncUI() {
    this.emit("ui-update");
  }
};
