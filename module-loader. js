// CORE MODULE LOADER (controls system features)
const ModuleLoader = {
  modules: {},

  register(name, module) {
    this.modules[name] = module;
  },

  get(name) {
    return this.modules[name];
  }
};
